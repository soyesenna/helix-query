package com.soyesenna.helixquery.entity;

/**
 * Enum for order status to test enum field operations.
 */
public enum OrderStatus {
    PENDING,
    CONFIRMED,
    SHIPPED,
    DELIVERED,
    CANCELLED
}
