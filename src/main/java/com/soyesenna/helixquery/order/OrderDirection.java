package com.soyesenna.helixquery.order;

/**
 * Enumeration representing the direction of ordering.
 */
public enum OrderDirection {

    /**
     * Ascending order (smallest to largest, A to Z).
     */
    ASC,

    /**
     * Descending order (largest to smallest, Z to A).
     */
    DESC
}
